import React, {} from 'react';
import ReactDOM from 'react-dom';

class ConsumerList extends React.Component {
    render() {
        return <ul></ul>
    }
}

ReactDOM.render(<ConsumerList />, document.getElementById('consumerList'));